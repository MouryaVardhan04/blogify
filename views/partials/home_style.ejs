<style>
    body{
        background-image:url(https://images.unsplash.com/photo-1504610926078-a1611febcad3?fm=jpg&q=60&w=3000&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxzZWFyY2h8Mnx8YmxhY2slMjBhbmQlMjB3aGl0ZSUyMHRlY2hub2xvZ3l8ZW58MHx8MHx8fDA%3D);
        background-position: center;
        background-repeat: no-repeat;
        background-size: cover;
        background-color: black;
    }
    .cards-container {
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 20px;
        padding: 100px 5% 50px;
        box-sizing: border-box;
    }
    .special-card {
        flex: 1 1 100%;
        max-width: 100%;
        height: 400px;
        background-color: rgba(107, 95, 95, 0.7);
        border-radius: 40px;
        border: 3px solid rgba(255, 255, 255, 0.3);
        box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        padding: 0;
        margin-bottom: 20px;
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    .special-card img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 40px;
        transition: transform 0.5s, filter 0.5s;
    }
   .special-card p, .special-card a {
        color: white;
        font-size: 1em;
        position: absolute;
        opacity: 0;
        transition: opacity 0.5s;
        font-family: Georgia, serif;
    }
    .special-card h2{
        color: rgb(0, 0, 0);
        font-size: 1em;
        position: absolute;
        opacity: 0;
        transition: opacity 0.5s;
        font-family:'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;

    }
    .special-card p {
        top: 50%;
        transform: translateY(-50%);
    }
    .special-card h2 {
        bottom: 5%;
        transform: translateY(-50%);
        font-size: 2em;
        opacity: 0.7;
    }
    .special-card a {
        bottom: 2%;
        transform: translateY(-50%);
    }
    .special-card:hover img {
        transform: scale(1.1);
        filter: blur(5px);
    }
    .special-card:hover p, .special-card:hover h2, .special-card:hover a {
        opacity: 1;
    }
    .card {
        flex: 1 1 calc(33.333% - 20px);
        max-width: calc(33.333% - 20px);
        min-width: 300px;
        height: 400px;
        background-color: rgba(107, 95, 95, 0.7);
        border-radius: 40px;
        border: 3px solid rgba(255, 255, 255, 0.3);
        box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
        display: flex;
        align-items: center;
        justify-content: center;
        flex-direction: column;
        margin-top: 20px;
        padding: 0px;
        text-align: center;
        position: relative;
        overflow: hidden;
    }
    .card img {
        width: 100%;
        height: auto;
        margin-top: -30px;
        border-radius: 20px;
        transition: transform 0.5s, filter 0.5s;
    }
    .card p, .card a {
        color: white;
        padding: 10px 0;
        font-size: 1em;
        position: absolute;
        opacity: 0;
        transition: opacity 0.5s;
        font-family: Georgia, serif;
    }
    .card p {
        top: 50%;
        transform: translateY(-50%);
    }
    .card a {
        bottom: 10px;
    }
    .card:hover img {
        transform: scale(1.1);
        filter: blur(5px);
    }
    .card:hover p, .card:hover a {
        opacity: 1;
    }
    @media (max-width: 900px) {
        .card {
            flex: 1 1 calc(50% - 20px);
            max-width: calc(50% - 20px);
        }
    }
   
    h2 {
        color: white;
        text-transform: uppercase;
        padding: 20px 0;
        margin-top: 20px;
        font-size: 2em;
        text-align: center;
        font-family:'Lucida Sans', 'Lucida Sans Regular', 'Lucida Grande', 'Lucida Sans Unicode', Geneva, Verdana, sans-serif;
    }
    .view-more{
        display: flex;
        align-items: center;
        justify-content: center;
        border-top: 3px solid rgba(255, 255, 255, 0.3);
        border-bottom: 3px solid rgba(255, 255, 255, 0.3);
        width: 100%;
        height: 8vh;
        background-color: rgba(200, 195, 195, 0.7);
        margin-bottom: 50px;
    }
    .view-more a{
        color: white;
        text-decoration: none;
        font-size: 30px;
        padding: 10px;
        transition: 1s;
    }

    .view-more a:hover{
        font-size: 1.8vw;
    }

    @media (max-width : 600px) {
        .card{
            height: 300px;
            max-width: calc(33.333% - 100px);

        }
        .special-card{
            height: 350px;
            width: 80%;

        }
    }
    
</style>