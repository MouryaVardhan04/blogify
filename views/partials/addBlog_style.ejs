<style>
  body {
  background-image: url(https://e0.pxfuel.com/wallpapers/236/529/desktop-wallpaper-black-laptop-black-office.jpg);
  background-size: cover;
  background-repeat: no-repeat;
  background-position: center;
  background-color: rgb(0, 0, 0);
}

.container {
  width: 90vw;
  height: 100vh;
  background-color: rgba(0, 0, 0, 0.3);
  border-radius: 40px;
  border: 3px solid rgba(255, 255, 255, 0.3);
  box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  margin-top: 90px;
  padding-bottom: 30px;
  backdrop-filter: blur(80px); /* Add this line for blur effect */
}

h2 {
  color: rgb(218, 198, 198);
  text-transform: uppercase;
  padding: 20px 0;
  font-size: 2em;
  margin-top: 20px;
  font-family: fantasy;
}

.form-group {
  position: relative;
  width: 80vw;
  margin: 30px 0;
}

.form-group input[type="text"],
.form-group textarea {
  width: 100%;
  height: 50px;
  background: transparent;
  outline: none;
  font-size: 1.2em;
  color: white;
  padding: 0px 30px 0 10px;
  border: none;
  border-radius: 10px;
  padding: 10px;
  border: 2px solid white;
}

.form-group input[type="file"] {
  width: 100%;
  height: 50px;
  background: transparent;
  outline: none;
  font-size: 1.2em;
  color: white;
  padding: 0px 30px 0 10px;
  border: none;
  border-radius: 10px;
  padding: 10px;
}

.form-group input[type="text"] {
  border: 2px solid rgba(250, 248, 248, 0.7);
}

.form-group input[type="file"] {
  width: 30vh;
}

.coverImage {
  color: #f0f0f0;
  font-size: 1.3em;
}

.coverImage i {
  padding-right: 40px;
}

.form-group label {
  position: absolute;
  top: 50%;
  left: 10px;
  transform: translateY(-50%);
  font-size: 1.2em;
  color: #ffff;
  transition: 0.5s;
}

.form-group input:focus ~ label,
.form-group input:not(:placeholder-shown) ~ label {
  top: -10px;
  font-size: 1.3em;
}

span {
  color: red;
  font-size: 35px;
}

.form-group i {
  position: absolute;
  top: 50%;
  right: 20px;
  transform: translateY(-50%);
  color: white;
  font-size: 1.2em;
}

.form-group textarea {
  width: 100%;
  height: 200px;
  background: transparent;
  outline: none;
  font-size: 1.2em;
  color: white;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 10px;
  scrollbar-width: none;
}

p > a {
  color: #ffff;
  text-decoration: none;
  font-weight: 600;
}

p > a:hover {
  text-decoration: underline;
  font-style: italic;
}

#btn {
  float: right;
  width: 30%;
  height: 50px;
  border-radius: 40px;
  border: none;
  font-size: 1.2rem;
  text-transform: uppercase;
  font-weight: 600;
  margin: 10px 0;
  cursor: pointer;
  transition: 0.5s;
}

#btn:hover {
  background: transparent;
  border-bottom: 2px solid white;
  color: #ffff;
}

@media (max-width: 374px) {
  .container {
    width: 90vw;
    height: 110vh;
    background-color: rgba(0, 0, 0, 0.3);
    border-radius: 40px;
    border: 3px solid rgba(255, 255, 255, 0.3);
    box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    margin-top: 90px;
    padding-bottom: 30px;
  }
}


  </style>