<style>
 html, body {
    height: 100%;
    margin: 0;
    display: flex;
    flex-direction: column;
}

body {
    background-color: rgb(0, 0, 0);
    background-image: url(https://wallpapercave.com/wp/wp13411742.jpg);
    background-position: center;
    background-repeat: no-repeat;
    background-size: cover;
}

.container {
    width: 400px;
    height: 450px;
    background-color: rgba(40, 37, 37, 0.7);
    border-radius: 40px;
    border: 3px solid rgba(255, 255, 255, 0.3);
    box-shadow: 0 0 30px rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    margin: 200px auto;  /* Center the container horizontally */
}

.box1 {
    flex: 1; /* Allow .box1 to grow and fill the available space */
    display: flex;
    align-items: center;
    justify-content: center;
}

h2 {
    color: white;
    text-transform: uppercase;
    padding: 20px 0;
    font-size: 2em;
}

.form-group {
    position: relative;
    width: 330px;
    border-bottom: 2px solid white;
    margin: 30px 0;
}

.form-group input {
    width: 100%;
    height: 50px;
    background-color: transparent;
    outline: none;
    font-size: 1.2em;
    color: white;
    padding: 0px 30px 0 10px;
    border: 2px solid rgba(0, 0, 0, 0.1);
}

.form-group input:focus,
.form-group input:not(:placeholder-shown) {
    background-color: transparent; 
    color: white;
}

.form-group label {
    position: absolute;
    top: 50%;
    left: 10px;
    transform: translateY(-50%);
    font-size: 1.2em;
    color: #ffff;
    transition: 0.5s;
}

.form-group input:focus ~ label,
.form-group input:not(:placeholder-shown) ~ label {
    top: -10px;
    font-size: 1.3em;
}

.form-group i {
    position: absolute;
    top: 50%;
    right: 20px;
    transform: translateY(-50%);
    color: white;
    font-size: 1.2em;
}

p {
    text-align: center;
    color: #ffff;
    padding: 10px 0;
}

p > a {
    color: #ffff;
    text-decoration: none;
    font-weight: 600;
}

p > a:hover {
    text-decoration: underline;
    font-style: italic;
}

#error {
    color: red;
    text-align: center;
    margin-top: 10px;
}

#btn {
    width: 100%;
    height: 50px;
    border-radius: 40px;
    border: none;
    font-size: 1.5em;
    text-transform: uppercase;
    font-weight: 600;
    margin: 10px 0;
    cursor: pointer;
    transition: 0.5s;
}
.error{
    padding-left: 60px;
    color: rgb(200, 195, 195);
}
#btn:hover {
    background: transparent;
    border-bottom: 2px solid white;
    color: #ffff;
}
@media (max-width: 500px) { 
    .container {
        width: 300px;
        height: auto;
        margin: 180px auto;  /* Reduce margin to fit smaller screens */
        padding: 20px;  /* Add padding to maintain inner spacing */
    }

    h2 {
        font-size: 1.5em;  /* Reduce font size for smaller screens */
        padding: 15px 0;  /* Adjust padding */
    }

    .form-group {
        width: 100%;  /* Make the input fields full width */
        margin: 20px 0;  /* Reduce margin for better spacing */
    }

    .form-group input {
        height: 40px;  /* Reduce input height */
        font-size: 1em;  /* Adjust font size */
        padding: 0px 20px 0 10px;  /* Adjust padding */
    }

    .form-group label {
        font-size: 1em;  /* Adjust label font size */
    }

    .form-group i {
        font-size: 1em;  /* Adjust icon size */
        right: 15px;  /* Adjust icon position */
    }

    p {
        font-size: 0.9em;  /* Reduce font size of paragraph */
        padding: 5px 0;  /* Adjust padding */
    }

    #btn {
        height: 45px;  /* Reduce button height */
        font-size: 1.2em;  /* Adjust button font size */
    }

    #error {
        font-size: 0.9em;  /* Adjust error message font size */
    }
}


</style>
